import React from 'react';
import { Link } from 'react-router-dom';

const Add = () => (
   <div>
      <h1>Add new article!</h1>
      <Link to='/'>
        home
      </Link>
   </div>
);

export default Add;